# Ignore large CSV in data/; track scripts, requirements, and docs
data/*.csv
# Keep import script local (not shared)
data/import_full_dataset.py

# Cursor IDE debug session logs (NDJSON)
.cursor/debug*.log

# macOS Finder metadata
.DS_Store

.claude
.cursor